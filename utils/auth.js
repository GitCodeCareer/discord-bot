const config = require("../config.json");

class Auth {

   constructor(client) {
      this.client = client
   }

   isAdmin() {

   }

   isMod() {
   
   }

   isRole(role="") {

      return
   }
}

module.exports = {
   Auth
}